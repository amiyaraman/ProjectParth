<UserControl x:Class="BigBrems.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BigBrems.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BigBrems Dashboard" FontSize="24" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" Padding="15" CornerRadius="5" Margin="0,0,0,20" BorderBrush="#DDD" BorderThickness="1">
            <StackPanel Orientation="Horizontal">

                <StackPanel Margin="0,0,30,0">
                    <TextBlock Text="Select Test Run / Dataset" Foreground="Gray" Margin="0,0,0,5"/>
                    <ComboBox Width="250" Height="30"
                              ItemsSource="{Binding Datasets}"
                              SelectedItem="{Binding SelectedDataset}"
                              DisplayMemberPath="Name"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Select Sensor Channel" Foreground="Gray" Margin="0,0,0,5"/>

                    <ComboBox Width="250" Height="30"
                              ItemsSource="{Binding Channels}"
                              SelectedItem="{Binding SelectedChannel}"
                              DisplayMemberPath="Name">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="IsEnabled" Value="True"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedDataset}" Value="{x:Null}">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Style>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding DisplayData}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowBackground="White"
                  AlternatingRowBackground="#F9F9F9">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:HH:mm:ss\}}" Width="150"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="100" FontWeight="Bold"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>