<UserControl x:Class="BigBrems.Views.ChannelsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White" CornerRadius="10" Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.04" BlurRadius="15" ShadowDepth="3"/>
        </Border.Effect>

        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="{DynamicResource StrChannels}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="#1E293B"/>

            <CheckBox DockPanel.Dock="Top" Content="Select All" 
                      IsChecked="{Binding IsAllChannelsSelected, Mode=TwoWay}" 
                      Margin="5,0,0,10" FontWeight="SemiBold" Foreground="#2563EB" Cursor="Hand"/>

            <Border BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Background="#F8FAFC">
                <ListBox ItemsSource="{Binding DisplayedChannels}" 
                         BorderThickness="0" Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Margin="2">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="10,8" Cursor="Hand">
                                <TextBlock Foreground="#334155" FontSize="14">
                                    <Run Text="{Binding Name}"/>
                                    <Run Text=" [" Foreground="Gray"/>
                                    <Run Text="{Binding Unit}" Foreground="Gray"/>
                                    <Run Text="]" Foreground="Gray"/>
                                </TextBlock>
                            </CheckBox>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </DockPanel>
    </Border>
</UserControl>