<UserControl x:Class="BigBrems.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BigBrems.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#F5F5F5">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BigBrems Analytics Dashboard" 
                   FontSize="28" 
                   FontWeight="SemiBold" 
                   Foreground="#333" 
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" Padding="15" CornerRadius="8" Margin="0,0,0,15" BorderBrush="#E0E0E0" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="1. Select Data Pool (Region/Lab)" FontSize="12" FontWeight="Bold" Foreground="#777" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding DataPools}"
                          SelectedItem="{Binding SelectedDataPool}"
                          DisplayMemberPath="Name"
                          Height="40" 
                          HorizontalAlignment="Stretch"
                          Padding="10,0" 
                          VerticalContentAlignment="Center"
                          Background="White"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="2. Select Datasets" FontSize="12" FontWeight="Bold" Foreground="#777" Margin="15,10,0,5"/>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Datasets}" Height="150" SelectionMode="Multiple" BorderThickness="0" Margin="5">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" 
                                          IsChecked="{Binding IsSelected}" 
                                          Margin="0,3"
                                          FontSize="14"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="3. Select Channels" FontSize="12" FontWeight="Bold" Foreground="#777" Margin="15,10,0,5"/>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Channels}" Height="150" SelectionMode="Multiple" BorderThickness="0" Margin="5">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" 
                                          IsChecked="{Binding IsSelected}"
                                          Margin="0,3"
                                          FontSize="14"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <DataGrid Grid.Row="3" 
                  ItemsSource="{Binding DisplayData}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="35"
                  ColumnHeaderHeight="40"
                  AlternatingRowBackground="#FAFAFA"
                  Background="White"
                  BorderThickness="0">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#EEE"/>
                    <Setter Property="Padding" Value="10,0"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:HH:mm:ss\}}" Width="120"/>
                <DataGridTextColumn Header="Channel Name" Binding="{Binding ChannelName}" Width="250"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="100" FontWeight="Bold"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>